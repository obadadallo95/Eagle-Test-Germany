# 🔐 ملاحظات الأمان (Security Notes)

## ⚠️ معلومات حساسة محمية

هذا الملف يوثق المعلومات الحساسة التي يجب حمايتها في المشروع.

---

## 🔑 كلمات المرور (Passwords)

### Keystore Passwords
- **الموقع**: `android/key.properties`
- **الحماية**: ✅ محمي في `.gitignore`
- **الملف**: `android/upload-keystore.jks`
- **الحماية**: ✅ محمي في `.gitignore`

**⚠️ مهم**: 
- لا ترفع `android/key.properties` إلى Git
- لا ترفع `android/upload-keystore.jks` إلى Git
- احفظ نسخة احتياطية آمنة من الملفات وكلمات المرور

---

## 🔐 API Keys

### RevenueCat API Key
- **الموقع**: `lib/core/services/subscription_service.dart`
- **النوع**: Test API Key (للتطوير)
- **القيمة**: `test_rFXIKCHeRdDYqpessQtlqMacqlT`
- **الحماية**: ⚠️ موجود في الكود (يجب نقله إلى متغيرات البيئة في الإنتاج)

**⚠️ ملاحظات**:
- هذا API Key هو test key ويمكن استخدامه للتطوير
- في الإنتاج، يجب نقل API Key إلى متغيرات البيئة أو ملف منفصل محمي
- لا ترفع API Keys الحقيقية إلى Git

### Gemini/Groq API Key
- **الموقع**: `lib/core/config/api_config.dart` (مثال: `api_config.example.dart`)
- **الحماية**: ✅ محمي في `.gitignore`
- **الملف**: `lib/core/config/api_config.dart` موجود في `.gitignore`

**⚠️ مهم**:
- استخدم `api_config.example.dart` كقالب
- انسخه إلى `api_config.dart` وأضف API Key الخاص بك
- لا ترفع `api_config.dart` إلى Git

---

## 📋 قائمة الملفات المحمية

### ملفات محمية في `.gitignore`:
- ✅ `*.jks` - جميع ملفات keystore
- ✅ `*.keystore` - جميع ملفات keystore
- ✅ `**/key.properties` - ملفات كلمات المرور
- ✅ `upload-keystore.jks` - ملف التوقيع الخاص
- ✅ `.env` - متغيرات البيئة
- ✅ `.env.local` - متغيرات البيئة المحلية
- ✅ `lib/core/config/api_config.dart` - ملف API Keys

---

## ✅ التحقق من الأمان

### قبل الرفع إلى Git:
- [ ] لا توجد كلمات مرور في ملفات `.md`
- [ ] لا توجد API Keys حقيقية في ملفات `.md`
- [ ] جميع الملفات الحساسة موجودة في `.gitignore`
- [ ] تم التحقق من أن الملفات الحساسة لم يتم رفعها: `git ls-files | grep -E "(key.properties|upload-keystore|api_config\.dart)"`

### بعد الرفع:
```bash
# التحقق من أن الملفات الحساسة لم يتم رفعها
git ls-files | grep -E "(key.properties|upload-keystore|api_config\.dart)" || echo "✅ آمن"
```

---

## 🚨 في حالة تسريب معلومات حساسة

إذا تم رفع معلومات حساسة عن طريق الخطأ:

1. **إزالة الملف من Git History**:
   ```bash
   git filter-branch --force --index-filter \
     "git rm --cached --ignore-unmatch android/key.properties" \
     --prune-empty --tag-name-filter cat -- --all
   ```

2. **تغيير كلمات المرور/API Keys**:
   - غيّر كلمة مرور keystore
   - أنشئ keystore جديد
   - غيّر API Keys في RevenueCat/Groq

3. **إجبار تحديث Remote**:
   ```bash
   git push origin --force --all
   ```

---

**📅 آخر تحديث**: 5 يناير 2025  
**✅ الحالة**: جميع المعلومات الحساسة محمية

